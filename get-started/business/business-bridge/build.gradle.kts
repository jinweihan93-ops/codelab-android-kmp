/*
 * business-ios-bridge
 *
 * Pure header/definition provider — no Kotlin sources, no compilation targets.
 * Artifacts:
 *
 *   businessBridge.def   — cinterop definition consumed by :business
 *   headers/             — ObjC protocol declarations consumed by the cinterop
 *
 * The actual cinterop compilation is configured in :business's build.gradle.kts.
 */

// No plugins — nothing to compile here.
